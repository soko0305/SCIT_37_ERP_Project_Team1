<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.erp.dao.MemberMapper">
<<<<<<< HEAD
 
 
	<insert id="insertMember" parameterType="com.project.erp.vo.MemberVO">
		INSERT INTO erp_member(
 				userid
				, userpw
				, userstatus
				, userdepartment
				, userstate
				, name
				, phonenumber
				, email
				, address
		)
		VALUES(
 				#{userid}
				, #{userpw}
				, #{userstatus}
				, #{userdepartment}
				, 'notalloewd'
				, #{name}
				, #{phonenumber}
				, #{email}
				, #{address}		
 
		)
	</insert>
		<select id="selectMemberById" parameterType="com.project.erp.vo.MemberVO" resultType="com.project.erp.vo.MemberVO">
		SELECT  userid
				, userpw
				, userstate
		FROM erp_member
		WHERE userid=#{userid}
	</select>
=======

>>>>>>> 4dda76466e554547e06e80ae06e8e1f4b2051fd2
</mapper>
